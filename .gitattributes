# 统一行尾与文本属性，避免跨平台协作踩坑：
# - shell 脚本必须是 LF：CRLF 会让 Linux / macOS 上出现 "set: pipefail: invalid option name" 之类的报错
# - Windows 脚本保持 CRLF：cmd 与 PowerShell 5.1 对 LF 的兼容性不稳定
*.sh text eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf

# 二进制与生成物按原样处理，不做行尾转换
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
